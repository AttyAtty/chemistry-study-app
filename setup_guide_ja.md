# Chemica 導入・編集ガイド

このガイドは、Windowsでプロジェクトを起動し、教材を追加し、最後にWeb公開するまでの手順です。

## 0. このアプリでできること

現時点では、次の機能があります。

1. 化学の大分類から単元を選ぶ
2. 表、カード、反応系統図で内容を確認する
3. 単元別または総合の四択テストを解く
4. 解答直後に正誤と解説を表示する
5. 学習記録をブラウザに保存する
6. `chemistry.ts` にデータを追加すると、画面へ自動反映する

最初から生徒ログインや教師用成績管理を入れていない理由は、教材データと画面構造を先に固めるためです。最初から全部入れると、問題内容を直すだけでもデータベース、権限、画面、通信処理を同時に触ることになり、修正が面倒になります。

---

## 1. インストールするもの

### 1-1. Node.js

1. Node.js公式サイトを開く
2. LTS版をダウンロードする
3. インストーラーを実行する
4. 基本的には初期設定のまま `Next` を押す
5. インストール後、Windowsを再起動する

PowerShellで次を実行します。

```powershell
node -v
npm -v
```

`node -v` が `v20.9.0` 以上なら利用できます。

### 1-2. Visual Studio Code

1. Visual Studio Codeをインストールする
2. 起動する
3. 上部メニューの「ファイル」→「フォルダーを開く」を押す
4. `chemistry-study-app` フォルダーを選ぶ

---

## 2. 最初の起動

### 一番簡単な方法

プロジェクト内の次のファイルをダブルクリックします。

```text
00_install_and_start.bat
```

初回は必要なパッケージをダウンロードするため、少し処理があります。PowerShellまたは黒い画面に次の表示が出れば起動成功です。

```text
Local: http://localhost:3000
```

ブラウザで次を開きます。

```text
http://localhost:3000
```

### PowerShellから起動する方法

```powershell
cd "C:\プロジェクトを置いた場所\chemistry-study-app"
npm install
npm run dev
```

終了するときは、起動した画面で `Ctrl + C` を押します。

2回目以降は、次のファイルだけで起動できます。

```text
01_start.bat
```

---

## 3. フォルダー構成

```text
chemistry-study-app
├─ src
│  ├─ app
│  │  ├─ page.tsx                 トップページ
│  │  ├─ quiz/page.tsx            テスト画面
│  │  ├─ progress/page.tsx        学習記録画面
│  │  └─ units/[slug]/page.tsx    各単元の資料画面
│  ├─ components
│  │  ├─ HomeClient.tsx           単元検索と一覧
│  │  ├─ QuizClient.tsx           テスト処理
│  │  ├─ ProgressClient.tsx       学習記録処理
│  │  └─ StudySection.tsx         表・カード・系統図の表示
│  ├─ data
│  │  └─ chemistry.ts             教材と問題の本体
│  └─ lib
│     └─ progress.ts              学習記録の保存処理
├─ docs                            説明書
├─ package.json                    使用パッケージと実行コマンド
└─ README.md                       概要
```

教材を増やす作業では、基本的に次の1ファイルだけを編集します。

```text
src/data/chemistry.ts
```

---

## 4. 問題を追加する

例として「硫化銅(II)の色」の問題を追加します。

1. VS Codeで `src/data/chemistry.ts` を開く
2. `slug: "colors-and-tests"` を検索する
3. その単元内の `questions: [` を探す
4. 既存の問題の後ろに、カンマを忘れず追加する

```ts
{
  id: "color-7",
  prompt: "硫化銅(II)の沈殿の色はどれですか。",
  choices: ["白色", "黒色", "黄色", "赤褐色"],
  answerIndex: 1,
  explanation: "CuSは黒色沈殿です。",
  tags: ["沈殿", "硫化物"],
},
```

### 各項目の意味

- `id`: 他の問題と重複しない名前
- `prompt`: 問題文
- `choices`: 選択肢。現在は4択を推奨
- `answerIndex`: 正解番号。0から数える
- `explanation`: 解答後に表示する解説
- `tags`: 後で苦手分析や絞り込みに利用できる分類

### answerIndexの数え方

```text
0 → 1番目
1 → 2番目
2 → 3番目
3 → 4番目
```

人間向けの番号が1からなのに、配列が0から始まるのはプログラミング界の古い慣習です。ここだけは毎回確認してください。

---

## 5. 学習用の表を追加する

`kind: "table"` のセクションでは、`columns` が列名、`rows` が表の内容です。

```ts
{
  id: "complex-colors",
  title: "錯イオンの色",
  kind: "table",
  columns: ["錯イオン", "色", "生成条件"],
  rows: [
    ["[Cu(NH₃)₄]²⁺", "深青色", "Cu²⁺に過剰のNH₃水"],
    ["[Ag(NH₃)₂]⁺", "無色", "AgClがNH₃水に溶解"],
  ],
},
```

`columns` が3個なら、各 `rows` も3個の文字列にそろえます。

---

## 6. 反応系統図を追加する

現在の系統図は、横方向のノードと矢印で表示します。

```ts
{
  id: "methane-flow",
  title: "メタンの置換反応",
  kind: "flow",
  flows: [
    {
      title: "塩素置換",
      nodes: ["CH₄", "CH₃Cl", "CH₂Cl₂", "CHCl₃", "CCl₄"],
      note: "光を当てると段階的に水素が塩素へ置換されます。",
    },
  ],
},
```

もっと複雑な枝分かれ図が必要になった場合は、将来 `React Flow` やSVGを使う専用表示へ変更します。最初はデータ入力が簡単な直線型にしてあります。

---

## 7. 新しい単元を追加する

`chemistryUnits` 配列の最後に、新しいオブジェクトを追加します。

```ts
{
  slug: "chemical-equilibrium",
  title: "化学平衡",
  shortTitle: "化学平衡",
  icon: "⚖️",
  summary: "平衡定数と平衡移動を学びます。",
  level: "高校化学",
  keywords: ["平衡", "ルシャトリエ", "平衡定数"],
  sections: [
    {
      id: "equilibrium-basics",
      title: "平衡移動の基本",
      kind: "cards",
      entries: [
        {
          title: "濃度変化",
          body: "加えた物質を減らす向きへ平衡が移動します。",
        },
      ],
    },
  ],
  questions: [
    {
      id: "eq-1",
      prompt: "平衡状態で反応物を加えたとき、一般に平衡はどちらへ移動しますか。",
      choices: ["反応物を増やす向き", "加えた反応物を減らす向き", "必ず変化しない", "反応が停止する"],
      answerIndex: 1,
      explanation: "ルシャトリエの原理により、加えた変化を打ち消す向きへ移動します。",
      tags: ["ルシャトリエ"],
    },
  ],
},
```

追加後は、トップページ、単元ページ、テスト選択画面へ自動的に表示されます。ルーティング用のファイルを単元ごとに作る必要はありません。

---

## 8. デザインを変更する

全体の色や余白は次のファイルにあります。

```text
src/app/globals.css
```

先頭の変数を変えると、主要な色をまとめて変更できます。

```css
:root {
  --primary: #2457e6;
  --accent: #14a38b;
  --bg: #f4f7fb;
}
```

---

## 9. エラー確認

### 文法と書き方の確認

```powershell
npm run lint
```

### 公開可能な状態か確認

```powershell
npm run build
```

この2つがエラーなしなら、公開前の基本確認は通っています。

付属の次のファイルをダブルクリックしても確認できます。

```text
02_check_build.bat
```

---

## 10. GitHubへ保存する

GitHub Desktopを使う方法が比較的簡単です。

1. GitHub Desktopをインストールする
2. `File` → `Add local repository` を押す
3. `chemistry-study-app` を選ぶ
4. リポジトリが未作成なら作成する
5. 左下に変更内容の説明を書く
6. `Commit to main` を押す
7. `Publish repository` を押す
8. 公開したくない場合は `Keep this code private` を有効にする

教材データを直したら、そのたびにコミットしておくと、間違った変更を元へ戻せます。

---

## 11. VercelでWeb公開する

1. VercelへGitHubアカウントでログインする
2. `Add New` → `Project` を押す
3. GitHubへ保存したリポジトリを選ぶ
4. `Import` を押す
5. Framework Presetが `Next.js` であることを確認する
6. Build Commandなどは基本的に変更しない
7. `Deploy` を押す
8. 発行されたURLを生徒へ共有する

GitHubの `main` ブランチへ新しい変更をpushすると、通常はVercel側で自動的に再公開されます。

---

## 12. 現在の学習記録の限界

現在はブラウザの `localStorage` に保存しています。

### 保存されるもの

- 単元別の受験回数
- 総正解数
- 総問題数
- 最高正答率
- 最終学習日時

### 保存されない・共有されないもの

- 別の端末への引き継ぎ
- ブラウザを変えた場合の共有
- 講師側からの成績閲覧
- 生徒名との紐付け

まず教材として使えるか確認し、その後にログインとデータベースを入れるのが安全です。

---

## 13. 次に実装する順序

### 第1段階：現在

- 学習資料
- 四択問題
- 即時解説
- 端末内進捗
- Vercel公開

### 第2段階：教材入力を楽にする

- CSVから問題を一括登録
- 教師用の問題編集画面
- 画像アップロード
- 問題の公開・非公開設定

### 第3段階：生徒管理

- 生徒ログイン
- クラス作成
- 課題配信
- 締切設定
- 生徒別・単元別の成績表示

### 第4段階：暗記支援の強化

- 間違えた問題だけ再出題
- 忘却曲線に合わせた復習
- 穴埋め問題
- 反応式を並べ替える問題
- 化学式を直接入力する問題
- 苦手タグ分析

---

## 14. 最初に追加すべき教材の優先順位

1. 現在塾で実際に配っている反応系統図
2. 生徒が頻繁に間違える色・沈殿
3. 電池の正極・負極・電子の流れ
4. 工業的製法の原料・触媒・条件
5. イオン反応式の基本パターン
6. 入試問題から抽出したひっかけ選択肢

市販参考書を丸ごと移植するのではなく、自分の授業で間違いが多かった部分から入れる方が、教材としての価値が上がります。
